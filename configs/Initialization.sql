CREATE TABLE HISTRECORDS(
    DATE TEXT,
    TEMP REAL,
    HUM REAL
);

CREATE TABLE EVENTS(
    START_TEMP REAL,
    PERIODIC TEXT,
    START_TIME TEXT,
    END_TIME TEXT,
    ACTIVE INTEGER
);

CREATE TABLE USAGE(
    START_DATE TEXT,
    END_DATE TEXT
);
